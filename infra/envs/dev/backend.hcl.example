# Copy to backend.hcl after `make bootstrap`, filling in the bootstrap outputs:
#   terraform init -backend-config=backend.hcl
bucket         = "aegis-tfstate-<ACCOUNT_ID>"
region         = "us-east-1"
dynamodb_table = "aegis-tf-lock"
